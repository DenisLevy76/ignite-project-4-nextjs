import { styled } from '../../styles';

export const HeaderComponentContainer = styled('header', {
  display: 'flex',
  alignItems: 'center',
  padding: '2rem 0',
  width: '100%',
  maxWidth: 1180,
  margin: '0 auto'
})