import { createStitches } from '@stitches/react'

export const { config,
  prefix,
  theme,
  createTheme,
  css,
  getCssText,
  globalCss,
  keyframes,
  reset,
  styled,
} = createStitches({
  theme: {
    colors: {
      rocktseat: '#8257e6'
    }
  }
})